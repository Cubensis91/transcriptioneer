{
  "name": "@transcriptioneer/ai",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Server-only AI services (transcription, extraction, analysis, embeddings, retrieval, chat). Must never be imported by apps/web or apps/mobile.",
  "type": "commonjs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@transcriptioneer/config": "workspace:*"
  }
}
